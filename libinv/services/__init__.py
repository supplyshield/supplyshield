"""Service layer — modules that orchestrate business logic on top of the ORM.

The model layer (`libinv/models.py`) should stay focused on data and
relationships. Cross-cutting concerns like report rendering, external
integration handlers, and multi-step workflows live here.
"""
